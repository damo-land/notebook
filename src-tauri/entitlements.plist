<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Signing entitlements for the app AND its bundled externalBin executables
     (node, claude): both embed JIT runtimes (V8 / Bun-JSC) and break under a
     hardened-runtime signature without these. The Tauri bundler applies this
     plist when it signs externalBin. See docs/reports/consumer-dmg-t1-packaging.md (Q3). -->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
</dict>
</plist>
